services:
  - type: web
    name: fpl-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.fpl.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.13"
